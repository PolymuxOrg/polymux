# Polymux GitHub Action

This app will remain a thin wrapper around the Polymux CLI and runtime. It will
prepare the execution environment, run flows against a local or preview
deployment, upload evidence artifacts, and report the result as a GitHub check.
